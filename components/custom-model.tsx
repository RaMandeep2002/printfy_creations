'use client';
import { ChangeEvent, useMemo, useState } from 'react';
import { Check, ChevronDown, ImagePlus, LoaderCircle, Upload, X } from 'lucide-react';
import { AddOn, CustomModelOrder, Material, ModelColor, ModelSize, ModelStyle, ModelType } from '@/types/product';
import { calculateCustomModelPrice, pricing } from '@/lib/pricing';
import { useStore } from './store-provider';
const modelTypes: ModelType[] = ['Portrait','Bust','Full Body','Couple','Family','Pet','Character']; const styles: ModelStyle[] = ['Realistic','Cartoon','Chibi','Minimal','Custom']; const sizes: ModelSize[] = ['Small','Medium','Large']; const materials: Material[] = ['PLA','Resin','Premium Resin']; const colors: ModelColor[] = ['White','Black','Custom']; const addons: AddOn[] = ['Premium Base','Name Plate','Custom Text','Gift Packaging']; const money = (n: number) => `₹${n.toLocaleString('en-IN')}`;
export function CustomModel() { const { addCustomModel } = useStore(); const [file, setFile] = useState<{ url: string; name: string } | null>(null); const [error, setError] = useState(''); const [processing, setProcessing] = useState(false); const [modelType, setModelType] = useState<ModelType>('Portrait'); const [style, setStyle] = useState<ModelStyle>('Realistic'); const [size, setSize] = useState<ModelSize>('Medium'); const [material, setMaterial] = useState<Material>('Resin'); const [color, setColor] = useState<ModelColor>('White'); const [selectedAddons, setSelectedAddons] = useState<AddOn[]>([]); const [customText, setCustomText] = useState(''); const [notes, setNotes] = useState(''); const price = useMemo(() => calculateCustomModelPrice(size, material, selectedAddons), [size, material, selectedAddons]); const handleFile = (e: ChangeEvent<HTMLInputElement>) => { const picked = e.target.files?.[0]; if (!picked) return; if (!['image/jpeg','image/png','image/webp'].includes(picked.type)) return setError('Please upload a JPG, PNG, or WEBP image.'); if (picked.size > 10 * 1024 * 1024) return setError('That image is over 10MB. Please choose a smaller file.'); setError(''); setFile({ url: URL.createObjectURL(picked), name: picked.name }); setProcessing(true); window.setTimeout(() => setProcessing(false), 1100); }; const toggleAddon = (item: AddOn) => setSelectedAddons(a => a.includes(item) ? a.filter(x => x !== item) : [...a, item]); const submit = () => { if (!file) return setError('Please upload a picture to continue.'); const order: CustomModelOrder = { image: file.url, fileName: file.name, modelType, style, size, material, color, addOns: selectedAddons, customText, customerNotes: notes, calculatedPrice: price }; addCustomModel(order); }; return <section id="custom" className="relative overflow-hidden border-b line py-24"><div className="absolute inset-0 bg-[radial-gradient(circle_at_15%_60%,rgba(139,232,255,.08),transparent_33%),radial-gradient(circle_at_90%_20%,rgba(164,139,255,.08),transparent_30%)]"/><div className="relative mx-auto max-w-[1320px] px-5 lg:px-8"><div className="max-w-[700px]"><div className="eyebrow">Custom build / model config</div><h2 className="serif mt-4 text-4xl tracking-[-.035em] sm:text-5xl">Create your custom <em className="text-cyan">3D model.</em></h2><p className="mt-4 leading-7 text-muted">Upload a picture. Customize your creation. We’ll turn it into something you can hold.</p></div><div className="mt-12 grid gap-5 lg:grid-cols-[.92fr_1.08fr]"><div className="rounded-3xl border line bg-[#10151d] p-4 sm:p-6"><div className="mb-5 flex items-center justify-between"><div className="eyebrow">01 / Photo input</div><div className="text-[10px] tracking-[.15em] text-muted">JPG · PNG · WEBP / 10MB</div></div>{file ? <div className="relative aspect-[1.25] overflow-hidden rounded-2xl bg-black"><img src={file.url} alt="Selected preview" className="h-full w-full object-contain"/><div className="absolute inset-0 bg-gradient-to-t from-black/70 to-transparent"/><div className="absolute bottom-4 left-4 right-4 flex items-center justify-between"><div><div className="text-[10px] uppercase tracking-widest text-cyan">Photo received</div><div className="mt-1 max-w-[220px] truncate text-sm">{file.name}</div>{processing && <div className="mt-1 flex items-center gap-1 text-xs text-muted"><LoaderCircle size={11} className="animate-spin"/> Preparing your customization...</div>}</div><button onClick={() => setFile(null)} className="grid h-9 w-9 place-items-center rounded-full bg-white/10" aria-label="Remove uploaded image"><X size={16}/></button></div></div> : <label className="grid aspect-[1.25] cursor-pointer place-items-center rounded-2xl border border-dashed border-white/20 bg-white/[.025] text-center transition hover:border-cyan/60 hover:bg-cyan/[.03]"><input type="file" accept="image/jpeg,image/png,image/webp" onChange={handleFile} className="sr-only"/><div><div className="mx-auto grid h-14 w-14 place-items-center rounded-full border line bg-white/5 text-cyan"><ImagePlus size={23}/></div><div className="mt-5 font-medium">Upload your picture</div><div className="mt-2 text-sm text-muted">Drag & drop your image here<br/>or browse from your device</div><span className="mt-5 inline-flex items-center gap-2 rounded-full bg-white px-4 py-2.5 text-xs font-bold text-black"><Upload size={14}/> Choose picture</span></div></label>}{error && <p className="mt-3 text-xs text-red-300">{error}</p>}<div className="mt-5 flex gap-3 text-xs text-muted"><Check size={15} className="text-cyan"/> Your image stays private and is only used to prepare your design.</div></div><div className="rounded-3xl border line bg-[#10151d] p-4 sm:p-6"><div className="mb-6 flex items-center justify-between"><div className="eyebrow">02 / Tune your creation</div><span className="text-xs text-muted">All prices include GST</span></div><div className="grid gap-5 sm:grid-cols-2">{[["Model type", modelTypes, modelType, setModelType],["Style", styles, style, setStyle],["Size", sizes, size, setSize],["Material", materials, material, setMaterial],["Color", colors, color, setColor]].map(([label, options, value, setter]) => <label key={label as string} className="relative block"><span className="mb-2 block text-xs font-medium text-slate-300">{label as string}</span><select value={value as string} onChange={e => (setter as (v: string) => void)(e.target.value)} className="w-full appearance-none rounded-xl border line bg-white/[.04] px-3.5 py-3 text-sm text-white outline-none focus:border-cyan">{(options as string[]).map(option => <option className="bg-[#131821]" key={option}>{option}</option>)}</select><ChevronDown size={15} className="pointer-events-none absolute right-3 top-9 text-muted"/></label>)}</div><div className="mt-6"><span className="mb-3 block text-xs font-medium text-slate-300">Add-ons</span><div className="grid gap-2 sm:grid-cols-2">{addons.map(item => <label key={item} className="flex cursor-pointer items-center gap-3 rounded-xl border line bg-white/[.025] p-3 text-xs text-slate-300 hover:border-white/25"><input type="checkbox" checked={selectedAddons.includes(item)} onChange={() => toggleAddon(item)} className="h-4 w-4 accent-cyan"/>{item}<span className="ml-auto text-muted">+{money(pricing.addOn[item])}</span></label>)}</div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-xs font-medium text-slate-300">Custom text <span className="text-muted">(optional)</span></span><input value={customText} onChange={e => setCustomText(e.target.value.slice(0,40))} placeholder="e.g. Forever" className="w-full rounded-xl border line bg-white/[.04] px-3.5 py-3 text-sm outline-none placeholder:text-muted focus:border-cyan"/></label><label><span className="mb-2 block text-xs font-medium text-slate-300">Customer notes <span className="text-muted">(optional)</span></span><input value={notes} onChange={e => setNotes(e.target.value.slice(0,200))} placeholder="Tell us anything special..." className="w-full rounded-xl border line bg-white/[.04] px-3.5 py-3 text-sm outline-none placeholder:text-muted focus:border-cyan"/></label></div><div className="mt-7 flex items-end justify-between border-t line pt-5"><div><div className="text-xs text-muted">Estimated total</div><div className="mt-1 text-3xl font-semibold tracking-tight">{money(price)}</div></div><button onClick={submit} className="rounded-full bg-cyan px-5 py-3 text-sm font-bold text-black transition hover:bg-white">Continue to cart</button></div></div></div></div></section> }
