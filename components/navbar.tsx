'use client';
import Link from 'next/link';
import { Box, Menu, Search, ShoppingBag, UserRound, Heart, X } from 'lucide-react';
import { useState } from 'react';
import { useStore } from './store-provider';
export function Navbar() { const [open, setOpen] = useState(false); const { cart, setCartOpen } = useStore(); return <header className="sticky top-0 z-50 border-b line bg-[#0b0e13]/90 backdrop-blur-xl"><div className="mx-auto flex h-[72px] max-w-[1320px] items-center justify-between px-5 lg:px-8"><Link href="/" className="focus-ring flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl border border-cyan/30 bg-cyan/10 text-cyan"><Box size={19}/></span><span className="text-[15px] font-bold tracking-[-.02em]">Printify <span className="text-cyan">Creations</span></span></Link><nav className="hidden items-center gap-7 text-[13px] text-slate-300 lg:flex"><Link className="hover:text-white" href="/">Home</Link><Link className="hover:text-white" href="#shop">Shop</Link><Link className="hover:text-white" href="#custom">Custom Model</Link><Link className="hover:text-white" href="#how">How It Works</Link><Link className="hover:text-white" href="#about">About</Link><Link className="hover:text-white" href="#contact">Contact</Link></nav><div className="hidden items-center gap-4 text-slate-300 md:flex"><button className="focus-ring hover:text-cyan" aria-label="Search"><Search size={18}/></button><button className="focus-ring hover:text-cyan" aria-label="Account"><UserRound size={18}/></button><button className="focus-ring hover:text-cyan" aria-label="Wishlist"><Heart size={18}/></button><button onClick={() => setCartOpen(true)} className="focus-ring relative hover:text-cyan" aria-label={`Cart, ${cart.length} items`}><ShoppingBag size={19}/>{cart.length > 0 && <span className="absolute -right-2 -top-2 grid h-4 min-w-4 place-items-center rounded-full bg-cyan px-1 text-[9px] font-bold text-black">{cart.length}</span>}</button><Link href="#custom" className="rounded-full bg-white px-4 py-2 text-xs font-bold text-black transition hover:bg-cyan">Create Your Model</Link></div><div className="flex items-center gap-4 md:hidden"><button onClick={() => setCartOpen(true)} className="relative" aria-label="Open cart"><ShoppingBag size={19}/>{cart.length > 0 && <span className="absolute -right-2 -top-2 grid h-4 min-w-4 place-items-center rounded-full bg-cyan text-[9px] text-black">{cart.length}</span>}</button><button onClick={() => setOpen(!open)} aria-label="Open menu">{open ? <X/> : <Menu/>}</button></div></div>{open && <nav className="border-t line bg-[#0b0e13] px-5 py-5 md:hidden"><div className="flex flex-col gap-5 text-sm text-slate-300"><Link onClick={() => setOpen(false)} href="/">Home</Link><Link onClick={() => setOpen(false)} href="#shop">Shop</Link><Link onClick={() => setOpen(false)} href="#custom">Custom Model</Link><Link onClick={() => setOpen(false)} href="#how">How It Works</Link><Link onClick={() => setOpen(false)} href="#about">About</Link></div></nav>}</header> }
